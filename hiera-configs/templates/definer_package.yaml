---

prefixes: 
    - "definer_package_"

classes:
  - definer

dependencies:
  - definer

definer::defs:
  package:
    "%{::definer_package_name}":
      ensure: "%{::definer_package_ensure}"
      provider: "%{::definer_package_provider}"
      source: "%{::definer_packag_source}"

