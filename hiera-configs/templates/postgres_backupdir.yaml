---

prefixes:
    - "postgres_"

classes:
    - database

dependencies:
  - database
  - mysqlconfig
  - postgresconfig
  - mysql
  - postgresql
  - stdlib
  - concat
  - apt

postgresconfig::backupdir: '%{::postgres_backup_dir}'
