---

sssd_ldap_default_authtok:
sssd_ldap_search_base: 
sssd_cert_name: 
sssd_ldap_default_bind_uid:
sssd_debug_level: '1'
sssd_ldap_server1: 
    type: 'nilable'
    value: 
    comment: 'Primary LDAP server to connect to'
sssd_ldap_server2: 
    type: 'nilable'
    value: 
    comment: 'Secondary LDAP server to connect to'

sssd_authconfig_command:
    value: 'authconfig --enablesssd --enablesssdauth --enablelocauthorize  --disableldap --disableldapauth --kickstart --update'
    comment: "Authconfig commnd to run on RedHat based systems"

sssd_custom_template: 
    type: 'nilable'
    value:
    comment: 'Custom ERB template to use instead of the default'
